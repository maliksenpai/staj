<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deneme.denemetahtasi2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Firebase.FirebaseActivity"></activity>
        <activity android:name=".Web.Web" />
        <activity android:name=".Web.HttpActivity" />
        <activity android:name=".Web.OkHttpActivity" />
        <activity android:name=".Retrofit.RetrofitActivity" />
        <activity android:name=".Sqlite.RoomActivity" />
        <activity android:name=".Bars.BarsActivity" />
        <activity android:name=".Async.AsyncActivity" />
        <activity android:name=".Glide.GlideActivity" />
        <activity android:name=".Dialog.DialogActivity" />
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>